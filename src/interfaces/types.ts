type NavElement = {
	navigation: any
}

type Feed = {
	userEmail: string,
	locationText: string
}

type Region = {
	latitude: number,
	longitude: number,
	latitudeDelta: number | 0.015,
	longitudeDelta: number | 0.0121,
}